import { styled } from "@/styles/stitches";

export const StyledLandingPageContainer = styled("div", {});

export const StyledLandingPageSection = styled("section", {
  position: "absolute",
  inset: 0,
  textAlign: "center",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  
});
